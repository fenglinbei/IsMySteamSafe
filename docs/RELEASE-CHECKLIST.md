# 正式公开发布检查清单

## 本次审查包已完成

- [x] v0.2.2-evidence 使用独立源码目录，不覆盖 v0.1 交付物。
- [x] App/Core 不引用 v0.1 Broker、ArchiveWorker、AMSI 或隔离模块。
- [x] 普通权限清单（`asInvoker`）。
- [x] 只读体检、URL 本地解析、报告导出与专业杀软交接。
- [x] 自动测试、真实 Steam 扫描与 Windows UI 实机验收。
- [x] 生成自包含 Windows x64 包与 SHA-256。

## 面向公众发布前必须完成

- [ ] 选择开源许可证并添加标准 `LICENSE`。
- [ ] 为 EXE 和安装/压缩包配置可信代码签名，记录证书主体与时间戳服务。
- [ ] 在干净 Windows 10、Windows 11 与至少一个非管理员账户上复测。
- [ ] 使用合法 Millennium、常见 Steam 皮肤、NVIDIA/Discord/RTSS 覆盖层建立误报语料。
- [ ] 使用脱敏的恶意夹具验证所有 P0 路径，避免在公共 CI 分发真实恶意样本。
- [ ] 独立安全审计 URL 解析、正则性能、长路径、符号链接和 TOCTOU 风险。
- [ ] 决定规则更新方案；如支持外部规则，必须有签名、回滚与最低安全基线，不能让可写规则静默关闭核心检测。
- [ ] 建立漏洞报告邮箱、安全政策和负责任披露流程。
- [ ] 复核 Microsoft .NET 自包含分发通知及所有名称/商标说明。
- [ ] 在公开说明中避免“绝对安全”“零误报”“100% 中毒”等无法保证的表述。

## 后续版本候选，不属于 v0.2.2-evidence 承诺

- 经过版本适配的 Steam 运行时 URL 映射读取。
- 可证明安全的 libcef 内存完整性检查。
- 预先启用且明确获用户同意的凭据文件访问审计。
- 签名的本地规则扩展包。
