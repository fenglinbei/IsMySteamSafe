# Changelog

## 0.2.6 - 2026-09-04

- 分离快速体检完成、风险结论和覆盖范围，修复正常内容边界被归因于权限、以及完成与未完成同时显示的问题。
- 新增折叠式检查范围、未深查内容分组、位置与原因，以及 SteamSentinel 完整内容扫描指引。
- 大视频先做格式与有界结构检查，不消耗整文件哈希预算，保留视频伪装和尾随可执行内容提示。
- 真正的读取失败与关键检查缺失仍明确保留，JSON 和 Markdown 新增覆盖记录与补查建议。
- 详见 docs/COVERAGE-0.2.6.md。

## 0.2.5 - 2026-09-04

- 扩展至本地全部 Steam 工坊 AppID，加入首批游戏 MOD、Steam 插件和关联落点检查。
- 根据实际文件身份关联恶意组件与启动/运行证据，保留两个工具的查杀与只读分工。
- 新增内容范围、覆盖缺口和隐私说明，沿用正常 GUI 文案、图标与签名流程。
- 详细实现、静态回归与待实机验收事项见 docs/COVERAGE-0.2.5.md。

## 0.2.4 - 2026-09-04

- 新增青绿色放大镜与盾牌应用图标，接入程序、主窗口、页头和安装器，提供多尺寸 ICO 和原始透明 PNG。
- 提供图标来源、生成提示词与 ICO 重建脚本，保留已验收的文案、快速检查和只读取证行为。
- 记录下一阶段全部本地 Steam 创意工坊关联与取证规划，扩展所有 App ID，但不加入递归查杀或处置功能，详见 `docs/ROADMAP.md`。

## 0.2.3 - 2026-09-04

- 将核对表中明确确认的界面、结果和报告文案补齐到源码，移除 EVIDENCE、开发进度及已标记删除的冗余展示。保留当前检测和只读取证能力。
- 补充开源与许可说明，版权主体为 fenglinbei，采用 Apache License 2.0。
- 新增当前用户安装包、快捷方式与卸载入口，安装和运行不要求管理员权限，不写入系统防火墙或证书信任。
- 支持显式证书签名，自有 EXE/DLL、安装器和卸载器均可签署，哈希在签名后生成，源码包排除 Git 元数据与私钥。

## 0.2.2-evidence · 2026-09-03

- 修复非主 Steam 库 Workshop 路径未被 Run/RunOnce 审计关联的问题；Wallpaper 内容建立 Windows 自启动时按高度可疑呈现。
- 新增运行中 Wallpaper/创意工坊程序检查；未签名或签名无效时给出强风险提示，但不由本工具处置。
- 只读取证包现会为运行中的 Wallpaper 内容程序记录 SHA-256 并尝试枚举相关模块，同时在 manifest 中记录 Steam 多库、Workshop 与本地 Wallpaper 项目根。
- 证据格式从 1.0 向后兼容扩展为 1.1；仍不复制二进制样本、不联网、不修改系统。

## 0.2.1-evidence · 2026-09-03

- 修正 JavaScript 常量布尔语义：`return !0` 为固定真值，`return !!0` 为固定假值。
- 新增客服 URL 局部变量映射解析、隐藏 URLBar 识别和 `steam.cfg` 成对禁更检查。
- 依据真实样本回归：识别强制红信、游戏动作重定向、`luminovastella.top` 路由和隐藏地址栏。
- 新增无需命令行、普通权限运行的只读取证 ZIP；覆盖进程树、模块、IPv4 TCP、启动链、服务、任务、代理/DNS、hosts、证书元数据和关键文件哈希。
- 证据包不复制二进制样本，默认脱敏用户目录、SteamID 与 URL 账户参数，并单列权限/瞬时覆盖限制。

## 0.2.0-review · 2026-09-03

- 产品更名为“我的 Steam 安全吗？”。
- 从通用扫描/处置工具转向 Steam 客户端垂直只读审计。
- 移除运行时对隔离、删除、回滚、提权 Broker、归档解包和 AMSI 扫描的依赖。
- 新增 DLL 侧载/转发结构、steamui 语义、客服路由、进程模块与持久化配置审计。
- 新增红信 URL 本地判真伪，处理 `@` 用户信息、相似后缀、非 HTTPS 与 IDN 主机显示。
- 新增专业杀毒软件交接流程与报告导出。
- 新增 Wallpaper Engine 工坊来源概览；它只作信息呈现，不判毒。
- 重做 WPF 界面，采用分级结论与三段式解释。

## 0.1.0-review

- 独立的旧版通用扫描与处置候选实现；未作为 v0.2.x 的运行时组成部分。
